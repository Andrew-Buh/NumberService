Number Service API
Микросервис для поиска N-го минимального числа в XLSX файлах с использованием эффективного алгоритма на основе max-heap.

📋 Оглавление
Особенности

Технологии

Требования

Сборка и запуск

Использование API

Алгоритм работы

Структура проекта

🚀 Особенности
🔍 Поиск N-го минимального числа в XLSX файлах

⚡ Эффективный алгоритм с временной сложностью O(M log N)

📊 Чтение данных из первого столбца Excel файлов

🛡 Полная валидация входных параметров

📚 Автоматическая документация Swagger

💾 Оптимальное использование памяти

🛠 Технологии
Java 17+

Spring Boot 3.2.0

Gradle - система сборки

Apache POI - работа с Excel файлами

MapStruct - маппинг DTO

Lombok - сокращение boilerplate кода

Swagger/OpenAPI 3 - документация API

Kotlin DSL - конфигурация Gradle

📋 Требования
JDK 17 или выше

Gradle 7.0+ (или используйте gradlew)

Доступ к файловой системе для чтения XLSX файлов

🏗 Сборка и запуск
1. Клонирование и настройка
bash
git clone <repository-url>
cd number-service
2. Сборка проекта (с использованием Gradle Wrapper)
Windows:

bash
gradlew.bat clean build
Linux/Mac:

bash
./gradlew clean build
3. Запуск приложения
bash
# Стандартный запуск
java -jar build/libs/number-service-1.0-SNAPSHOT.jar

# Запуск с кастомным портом
java -jar build/libs/number-service-1.0-SNAPSHOT.jar --server.port=8081

# Или через Gradle
./gradlew bootRun
4. Документация API
После запуска приложения документация доступна по адресу:

Swagger UI: http://localhost:8080/swagger-ui.html

OpenAPI JSON: http://localhost:8080/v3/api-docs

📖 Использование API
Эндпоинт
text
GET /api/numbers/nth-minimum?filePath=/path/to/file.xlsx&n=3
Параметры запроса
Параметр	Тип	Обязательный	Описание	Пример
filePath	string	✅	Абсолютный путь к XLSX файлу	/home/user/data/numbers.xlsx
n	integer	✅	Порядковый номер минимального числа (начинается с 1)	3
Примеры запросов
bash
# Поиск 3-го минимального числа
curl "http://localhost:8080/api/numbers/nth-minimum?filePath=/data/numbers.xlsx&n=3"

# Поиск 1-го минимального числа (минимума)
curl "http://localhost:8080/api/numbers/nth-minimum?filePath=/data/data.xlsx&n=1"
Примеры ответов
Успешный ответ (200 OK):

json
{
  "data": 5
}
Ошибка (400 Bad Request):

json
{
  "status": 400,
  "message": "Файл не существует: /invalid/path.xlsx"
}
🧠 Алгоритм работы
Общая схема
text
Запрос → Контроллер → Сервис → [Валидация → Чтение файла → Поиск числа] → Ответ
Детальное описание алгоритма
1. Валидация входных параметров
java
// Проверка существования файла
File file = new File(filePath);
if (!file.exists()) {
    throw new IllegalArgumentException("Файл не существует: " + filePath);
}

// Проверка формата файла
if (!filePath.toLowerCase().endsWith(".xlsx")) {
    throw new IllegalArgumentException("Файл должен быть в формате XLSX");
}

// Проверка значения N
if (n <= 0) {
    throw new IllegalArgumentException("N должно быть положительным числом");
}
2. Чтение данных из Excel
Чтение первого столбца XLSX файла

Игнорирование нечисловых значений

Сбор всех числовых значений в массив

3. Алгоритм поиска N-го минимального числа (Max-Heap)
Основная идея
Использование максимальной кучи (max-heap) для хранения N минимальных элементов, найденных на данный момент.

Псевдокод
text
function findNthMinimum(numbers, n):
    maxHeap = new PriorityQueue(Comparator.reverseOrder())

    for number in numbers:
        if maxHeap.size() < n:
            maxHeap.add(number)
        else if number < maxHeap.peek():
            maxHeap.poll()
            maxHeap.add(number)

    return maxHeap.peek()
Визуальный пример
Входные данные: [5, 3, 8, 1, 2], n = 3

text
Шаг 1: Добавить 5 → Heap: [5]
Шаг 2: Добавить 3 → Heap: [5, 3]
Шаг 3: Добавить 8 → Heap: [8, 3, 5] (корень=8)
Шаг 4: 1 < 8 → Удалить 8, добавить 1 → Heap: [5, 3, 1] → [5, 3, 1] (корень=5)
Шаг 5: 2 < 5 → Удалить 5, добавить 2 → Heap: [3, 2, 1] → [3, 2, 1] (корень=3)

Результат: 3
Сложность алгоритма
Временная сложность: O(M log N), где M - количество чисел, N - порядковый номер

Пространственная сложность: O(N) для хранения кучи

4. Преимущества алгоритма
✅ Эффективность памяти: хранит только N элементов

✅ Временная эффективность: не требует полной сортировки массива

✅ Онлайн обработка: может обрабатывать потоковые данные

✅ Устойчивость к дубликатам: корректно обрабатывает повторяющиеся значения

📁 Структура проекта
text
number-service/
├── src/main/java/com/example/
│   ├── controller/           # REST контроллеры
│   │   ├── NumberController.java         # Интерфейс с Swagger аннотациями
│   │   └── impl/NumberControllerImpl.java # Реализация контроллера
│   ├── service/              # Бизнес-логика
│   │   ├── NumberService.java            # Интерфейс сервиса
│   │   └── impl/NumberServiceImpl.java   # Реализация сервиса
│   ├── dto/                  # Data Transfer Objects
│   │   └── ResponseNumber.java           # DTO для ответа API
│   ├── mapper/               # Мапперы
│   │   └── NumberMapper.java             # Маппер для преобразования данных
│   ├── util/                 # Утилиты
│   │   ├── ExcelFileProcessor.java       # Обработчик Excel файлов
│   │   └── NumberFinder.java             # Алгоритм поиска чисел
│   ├── exception/            # Обработка ошибок
│   │   ├── handler/GlobalExceptionHandler.java
│   │   └── response/ErrorResponse.java
│   └── NumberServiceApplication.java     # Главный класс приложения
├── build.gradle.kts          # Конфигурация Gradle
├── settings.gradle.kts       # Настройки проекта
└── gradlew                   # Gradle Wrapper (Linux/Mac)
🧪 Пример тестового файла
Создайте XLSX файл test_data.xlsx с данными в первом столбце:

A	B	C
5	-	-
3	-	-
8	-	-
1	-	-
2	-	-
Запрос для этого файла:

bash
curl "http://localhost:8080/api/numbers/nth-minimum?filePath=/path/to/test_data.xlsx&n=3"
Ожидаемый ответ: {"data": 3}

⚠️ Ограничения
Поддерживаются только XLSX файлы (не XLS)

Читаются только числовые значения из первого столбца

Максимальный размер файла ограничен доступной памятью

Для очень больших файлов рекомендуется использовать потоковое чтение

🔧 Настройка
Создайте файл src/main/resources/application.properties для кастомизации:

properties
# Порт приложения
server.port=8080

# Уровень логирования
logging.level.com.example=INFO

# Настройки файлов
app.file.max-size=10MB
app.file.allowed-extensions=xlsx
🗂 Зависимости проекта
Основные зависимости указаны в build.gradle.kts:

spring-boot-starter-web - веб-функциональность

poi-ooxml - работа с Excel файлами

springdoc-openapi - документация API

lombok - генерация кода

mapstruct - маппинг объектов

🐛 Решение проблем
Проблема: "Не удается найти или загрузить главный класс"
bash
# Пересоберите проект
./gradlew clean build
Проблема: "Файл не существует"
Убедитесь, что указан абсолютный путь к файлу

Проверьте права доступа к файлу

Проблема: "Unsupported class file major version"
Убедитесь, что используется JDK 17+

📄 Лицензия
MIT License